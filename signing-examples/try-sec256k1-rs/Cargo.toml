[package]
name = "try-sec256k1-rs"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
secp256k1 = { version = "0.26", features = ["std", "bitcoin_hashes", "rand", "global-context"] }
hex = "0.4"
hex-literal = "0.3.4"
